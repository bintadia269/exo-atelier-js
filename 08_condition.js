//Demande à l'utilisateur d'entrer un nombre
let nombre = prompt('20');

// Vérifie si le nombre est positif, négatif ou nul

if (nombre > 0) {
    alert("Le nombre est positif.");
} else if (nombre < 0) {
    alert("Le nombre est négatif.");
} else {
    alert("Le nombre est nul.");
}
